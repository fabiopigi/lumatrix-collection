{
  "name": "lumatrix-simulator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node compile.mjs",
    "watch": "node compile.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
